# GENERATED by `python -m byakugan.gen_sources` — do not edit.
# Source: byakugan.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: zeek_dns
sensor_version: 0.1.0
sensor_developer: The Zeek Project
sensor_url: https://zeek.org
sensor_description: CAR events derived by Zeek (dns.log). Coverage is generated by introspecting this
  repo's map `zeek_dns` (byakugan.mappings); regenerate after any map change.
derived_from: network capture (pcap / live traffic)
extractor:
  tool: Zeek
  parser: dns.log
  url: https://zeek.org
input_pattern:
- dns.json
identity:
  external:
  - zeek_uid_trans_id
data_model_coverage: flow
mappings:
- object: flow
  action: message
  notes: Derived by Zeek — dns.log.
  fields:
  - application_protocol
  - dest_ip
  - dest_port
  - fqdn
  - src_ip
  - src_port
  - start_time
  - transport_protocol
  field_provenance:
    application_protocol: '[asserted]'
    dest_ip: id.resp_h [direct]
    dest_port: id.resp_p [direct]
    fqdn: query [direct]
    src_ip: id.orig_h [direct]
    src_port: id.orig_p [direct]
    start_time: ts [derived]
    transport_protocol: proto [direct]
other_coverage: []

# GENERATED by `python -m byakugan.gen_sources` — do not edit.
# Source: byakugan.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: zeek_ssl
sensor_version: 0.1.0
sensor_developer: The Zeek Project
sensor_url: https://zeek.org
sensor_description: CAR events derived by Zeek (ssl.log). Coverage is generated by introspecting this
  repo's map `zeek_ssl` (byakugan.mappings); regenerate after any map change.
derived_from: network capture (pcap / live traffic)
extractor:
  tool: Zeek
  parser: ssl.log
  url: https://zeek.org
input_pattern:
- ssl.json
identity:
  external:
  - zeek_uid
data_model_coverage: flow
mappings:
- object: flow
  action: message
  notes: Derived by Zeek — ssl.log.
  fields:
  - application_protocol
  - dest_fqdn
  - dest_ip
  - dest_port
  - src_ip
  - src_port
  - start_time
  - transport_protocol
  field_provenance:
    application_protocol: '[asserted]'
    dest_fqdn: server_name [direct]
    dest_ip: id.resp_h [direct]
    dest_port: id.resp_p [direct]
    src_ip: id.orig_h [direct]
    src_port: id.orig_p [direct]
    start_time: ts [derived]
    transport_protocol: proto [coalesced]
other_coverage: []

# GENERATED by `python -m byakugan.gen_sources` — do not edit.
# Source: byakugan.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: esedump_srum
sensor_version: 0.1.0
sensor_developer: Eric Zimmerman (EZ Tools)
sensor_url: https://github.com/Get-Sybers/EZTools-Docker
sensor_description: CAR events derived by ese_dump (SRUM (SRUDB.dat ESE database)). Coverage is generated
  by introspecting this repo's map `esedump_srum` (byakugan.mappings); regenerate after any map change.
derived_from: disk image (SRUDB.dat ESE database)
extractor:
  tool: ese_dump
  parser: SRUM (SRUDB.dat ESE database)
  url: https://github.com/Get-Sybers/EZTools-Docker
input_pattern:
- ApplicationResourceUsage
- NetworkDataUsage
identity:
  external:
  - esedump_srum_application
  - esedump_srum_network
data_model_coverage: flow, process
mappings:
- object: flow
  action: message
  notes: Derived by ese_dump — SRUM (SRUDB.dat ESE database).
  fields:
  - exe
  - image_path
  - in_bytes
  - out_bytes
  - uid
  field_provenance:
    exe: AppIdName | AppIdName [coalesced]
    image_path: AppIdName [inferred]
    in_bytes: BytesRecvd [direct]
    out_bytes: BytesSent [direct]
    uid: UserIdName [inferred]
- object: process
  action: create
  notes: Derived by ese_dump — SRUM (SRUDB.dat ESE database).
  fields:
  - exe
  - image_path
  - sid
  field_provenance:
    exe: AppIdName | AppIdName [coalesced]
    image_path: AppIdName [inferred]
    sid: UserIdName [inferred]
other_coverage: []
